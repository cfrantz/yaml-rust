--- # inlined document begin comment
# initial comment 1
# initial comment 2
# initial comment 3
# before level 1
- level1: # inline level 1
  # before first
  - first: # inline first
      # before 1
      - 1 # inline 1
      # before 2
      - 2 # inline 2
      # after 2
  # after first
  # before level 2
  - level2: # inline level 2
      # before second
      - second: # inline second
          # before 1
          - 1 # inline 1
          # before 2
          - 2 # inline 2
          # after 2
      # after second
      # before level 3
      - level3:
          # before third
          - third: # inline third
            # before 1
            - 1 # inline 1
            # before 2
            - 2 # inline 2
            # after 2
          # after third
      # after level 3
  # after level 2
# after level 1
# before last
- last: 1 # inline last
# after last
